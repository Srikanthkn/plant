from dataclasses import fields
from django.db import models

# Create your models here.# 
class sellingdetails(models.Model):
     fields

     class buyingdetails(models.Model):
           fields

           class donate(models.Model):
               fields